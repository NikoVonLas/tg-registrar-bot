# Быстрый старт (5 минут)

Пошаговая инструкция для запуска бота для вашего мероприятия.

## Шаг 1: Создание бота (2 минуты)

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду: `/newbot`
3. Придумайте имя бота: `Event Registration Bot`
4. Придумайте username: `your_event_bot` (должен заканчиваться на `bot`)
5. **Сохраните токен** (будет выглядеть примерно так):
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz12345678
   ```

## Шаг 2: Получение Admin ID (1 минута)

1. Откройте **@userinfobot** в Telegram
2. Отправьте любое сообщение
3. **Сохраните ваш User ID** (например: `987654321`)

## Шаг 3: Запуск бота (2 минуты)

### Вариант A: Через Bot Platform (рекомендуется)

```bash
# 1. Создать бота
curl -X POST http://localhost:3000/bots \
  -H "Content-Type: application/json" \
  -d '{
    "name": "event-registration",
    "runtime": "swarm",
    "sourceType": "local",
    "source": "./tgbots/prohodka"
  }'

# Сохраните "id" из ответа (например: abc123)

# 2. Запустить бота (замените abc123 на ваш ID)
curl -X POST http://localhost:3000/bots/abc123/start \
  -H "Content-Type: application/json" \
  -d '{
    "env": {
      "BOT_TOKEN": "ВСТАВЬТЕ_ВАШ_ТОКЕН_СЮДА",
      "ADMIN_USER_ID": "ВСТАВЬТЕ_ВАШ_USER_ID"
    }
  }'

# Готово! Бот запущен.
```

### Вариант B: Локально (для тестирования)

```bash
# 1. Перейти в папку проекта
cd prohodka

# 2. Установить зависимости (один раз)
npm install

# 3. Создать файл .env
cp .env.example .env

# 4. Отредактировать .env (вставить токен и ID)
nano .env

# В файле замените:
BOT_TOKEN=ваш_токен_из_шага_1
ADMIN_USER_ID=ваш_id_из_шага_2

# 5. Запустить
npm run dev

# Готово! Бот работает в режиме разработки.
```

## Шаг 4: Проверка работы

1. Откройте вашего бота в Telegram (по username из Шага 1)
2. Отправьте команду: `/start`
3. Выберите ваш город
4. Вы должны увидеть сообщение: "✅ Отлично! Вы зарегистрированы."

### Проверка админ-команд

1. Отправьте: `/stats`
2. Должны увидеть статистику с вашей регистрацией
3. Отправьте: `/export`
4. Должны получить CSV файл

## Шаг 5: Создание QR-кода

1. Откройте https://qr-code-generator.com
2. Выберите тип: **URL**
3. Вставьте ссылку: `https://t.me/your_event_bot?start=event`
   - Замените `your_event_bot` на username вашего бота
4. Скачайте QR-код (PNG, размер 300x300px минимум)
5. Распечатайте и разместите на мероприятии

### Пример готовой ссылки:
```
https://t.me/your_event_bot?start=event
```

## Готово! 🎉

Теперь:
- Участники сканируют QR → попадают в бота → регистрируются
- Вы отслеживаете статистику через `/stats`
- Экспортируете данные через `/export` для анализа

---

## Что дальше?

### Настройка для мероприятия

1. **Распечатайте QR-коды** (минимум 3-5 штук на мероприятие)
2. **Разместите на входе** - чтобы люди сканировали при входе
3. **Добавьте надпись**: "Отсканируйте для регистрации"

### Мониторинг в день события

1. Откройте бота на телефоне
2. Периодически отправляйте `/stats`
3. Смотрите количество регистраций в реальном времени

### После мероприятия

1. Отправьте `/export` - получите CSV
2. Откройте в Excel или Google Sheets
3. Анализируйте данные:
   - Сколько всего пришло
   - Из каких городов
   - В какое время регистрировались

---

## Помощь

### Бот не отвечает?

**Проверьте:**
1. Токен правильный (скопирован полностью)
2. Бот запущен (для Bot Platform: проверьте статус)
3. Webhook настроен (Bot Platform делает это автоматически)

**Посмотрите логи:**
```bash
# Для Bot Platform
curl http://localhost:3000/bots/abc123/logs

# Для локального запуска
# Смотрите в консоли где запускали npm run dev
```

### Админ-команды не работают?

**Проверьте:**
1. `ADMIN_USER_ID` установлен правильно
2. Ваш User ID совпадает с тем, что указали
3. Перезапустите бота с правильным ID

### Город не находится?

**Решение:**
1. Откройте `src/data/cities.json`
2. Добавьте город в алфавитном порядке
3. Пересоберите: `npm run build`
4. Перезапустите бота

---

## Дополнительно

- **Полная документация:** [README.md](./README.md)
- **Развёртывание:** [DEPLOYMENT.md](./DEPLOYMENT.md)
- **API команд:** [API.md](./API.md)
- **Архитектура:** [ARCHITECTURE.md](./ARCHITECTURE.md)

---

**Вопросы?** Смотрите [FAQ в README](./README.md#faq) или создайте issue в GitHub.
